# Component library — incident-retro v3 "The Learning Board"

Semantic tokens only. Type: Cabinet Grotesk / Supreme / Space Mono.

## Segmented nav (signature)
An inset graphite dock holding pill links; the active view fills solid lime
with dark ink. Brand: lime square + Cabinet Grotesk 800 wordmark.
Theme toggle at the end. States: default / hover / active (raised) / focus.

## Masthead
Lime dash eyebrow (mono caps) → Cabinet Grotesk 800 headline with the lime
lesson phrase → lede with bold key nouns.

## Form panel
Sticky 360px card: borderless inset fields that raise + lime-ring on
focus; 2-up grids for severity/users and downtime/SLA; mono textarea for the
incident; pill buttons (primary lime / ghost).

## The board document (engine-emitted, restyled)
- **Header**: mono POST-INCIDENT REVIEW label → Cabinet Grotesk 800 title
  (clamp to 2.2rem) → severity pill + mono stamp.
- **Blast radius (scoreboard)**: four inset tiles, Cabinet 800 numerals at
  clamp(1.6–2.2rem); breach tile red-ringed with "+18 min over SLA (30)",
  margin tile green-ringed; the lime-ruled truth line beneath.
- **Timeline**: mono lime times (700), baseline-aligned entries, hairline
  rows; gap chips as amber pills that wrap safely at 360px.
- **5-Whys**: mono `Why n` gutter + editable inset inputs.
- **Factors**: four labelled buckets (mono caps) with mono textareas.
- **Remediations**: inset cards — action input (raised field) + mono
  effort/impact selects + live **priority pill** (Quick win SOLID LIME / Major
  project blue / Planned amber / Fill-in neutral / Reconsider red).
- **Executive summary**: full-width editable area (template-filled or AI).
- **Reasoning receipt**: dashed-rule mono block naming model · task · time.
- **Doc bar**: Copy Markdown (primary) · Download · Save to library · Print.

## Library
Cabinet-titled cards with severity pill, mono meta line, action row; hover lifts
2px. Designed empty state.

## Empty states
Dashed clay frames, serif headings, plain guidance + the no-key reassurance
note ("Build RCA and the example both work without one").

## Reference
Cabinet 800 h2 prose — the "numbers are computed, not generated" contract, AI
usage, effort-vs-impact glossary, where it fits in the suite.
