# Component library — incident-retro "The Debrief"

Semantic tokens only. Type: Instrument Serif / Switzer / Space Mono.

## Segmented nav (signature)
An inset clay track holding pill links; the active view floats on a raised
card pill with shadow. Brand: oxblood dot + Instrument Serif wordmark.
Theme toggle at the end. States: default / hover / active (raised) / focus.

## Masthead
Oxblood dash eyebrow (mono caps) → Instrument Serif headline with *italic
emphasis* → lede with bold key nouns.

## Form panel
Sticky 360px card: borderless inset fields that raise + oxblood-ring on
focus; 2-up grids for severity/users and downtime/SLA; mono textarea for the
incident; pill buttons (primary oxblood / ghost).

## The debrief document (engine-emitted, restyled)
- **Header**: mono POST-INCIDENT REVIEW label → serif title (clamp to 2.4rem)
  → severity pill + mono stamp.
- **Blast radius**: four clay tiles, serif numerals at 2rem; breach tile
  red-washed with "+18 min over SLA (30)", margin tile green-washed; the
  italic truth line beneath.
- **Timeline**: mono oxblood times, baseline-aligned entries, hairline rows;
  gap chips as amber pills.
- **5-Whys**: mono `Why n` gutter + editable inset inputs.
- **Factors**: four labelled buckets (mono caps) with mono textareas.
- **Remediations**: inset cards — action input (raised field) + mono
  effort/impact selects + live **priority pill** (Quick win green / Major
  project blue / Planned amber / Fill-in neutral / Reconsider red).
- **Executive summary**: full-width editable area (template-filled or AI).
- **Reasoning receipt**: dashed-rule mono block naming model · task · time.
- **Doc bar**: Copy Markdown (primary) · Download · Save to library · Print.

## Library
Serif-titled cards with severity pill, mono meta line, action row; hover lifts
2px. Designed empty state.

## Empty states
Dashed clay frames, serif headings, plain guidance + the no-key reassurance
note ("Build RCA and the example both work without one").

## Reference
Serif h2 prose — the "numbers are computed, not generated" contract, AI
usage, effort-vs-impact glossary, where it fits in the suite.
